<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- إذن الإنترنت لإرسال الصورة لخدمة التعرف -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- إذن رسم الزر العائم فوق التطبيقات الأخرى -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- أذونات تسجيل/التقاط الشاشة -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_search_circle"
        android:label="@string/app_name"
        android:theme="@style/Theme.NameFinder"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- خدمة الزر العائم + التقاط الشاشة (تعمل بالخلفية) -->
        <service
            android:name=".FloatingButtonService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />

    </application>
</manifest>
